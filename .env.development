# Local development env (used by Vite)
VITE_BACKEND_URL="http://localhost:8000"
# Also set for compatibility with any remaining React scripts expecting REACT_APP_ prefix
REACT_APP_BACKEND_URL="http://localhost:8000"
